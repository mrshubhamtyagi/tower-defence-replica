using System;
using System.Collections;
using UnityEngine;

public class DefenderSpawner : MonoBehaviour
{
    public Tile tileScript;

    private void Update()
    {
        if (Input.GetMouseButtonDown(0) && Tile.isHovered)
        {

        }
    }
}
